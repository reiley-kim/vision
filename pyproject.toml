[tool.usort]

first_party_detection = false

[tool.black]

line-length = 120
target-version = ["py38"]

[tool.ufmt]

excludes = [
    "gallery",
]

dependencies = [
  "pytorch@git+https://github.com/pytorch/pytorch.git#egg=nightly",
]

[build-system]

requires = ["setuptools", "wheel"]
